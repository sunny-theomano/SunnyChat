# Copy to .env and fill in. This file is safe to commit.
#
# sunny-chat does not load these automatically — your app reads env and passes
# values into SunnyChat, e.g. baseUrl={import.meta.env.VITE_API_BASE}

# Vite / modern bundlers
VITE_API_BASE=

# Create React App (legacy name from Sunny backend docs)
# REACT_APP_V3_BASE_URL=
